version: '3'

services:

  api:
    image: api
    build: komtek-api/.
    command: manage.py runserver
    volumes:
      - ./komtek-api/:/usr/src/app/
    ports:
      - 8000:8000
    env_file:
      - ./komtek-api.env